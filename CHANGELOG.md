# Changelog

Tracks API contract drift detected by `contract-diff-analyzer` on every `/generate <spec> --sync` run. Entries are appended automatically — do not hand-edit above the newest entry.

<!-- New entries are inserted below this line by contract-diff-analyzer -->
